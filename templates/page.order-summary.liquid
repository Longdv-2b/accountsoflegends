<div class="page-order-summary">
  {% section 'page-order-summary' %}
</div>
<style>
.label-email {
  color: #D0A85C;
  font-size: 13px;
  margin-bottom: 5px;
  font-style: italic;
}
@keyframes sk-scaleout {
  0% {
    -webkit-transform: scale(0);
    transform: scale(0);
  }
  100% {
    -webkit-transform: scale(1);
    transform: scale(1);
    opacity: 0;
  }
}
body.loaded .preloader {
  display: none;
}
.preloader-spinner {
  width: 40px;
  height: 40px;
  background-color: #e42555;
  border-radius: 50%;
  position: relative;
  top: 50%;
  margin: -20px auto auto auto;
  -webkit-animation: sk-scaleout 1s infinite ease-in-out;
  animation: sk-scaleout 1s infinite ease-in-out;
}
.preloader {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: #ffffff;
  z-index: 13000;
  height: 100%;
}
.footer-bottom {
  background-color:white;
}
body {
background-color: #f1f5fe;
}
.form-os .btn{
  background-color: #313131;
  color: white;
  padding: 0 22px;
  margin-left: 17px;
}
.sm-data {
  background-color: #D0A85C;
  border: none;
  width: 100%;
  padding: 20px;
  font-size: 20px;
  color: white;
  margin-top: 30px;
  border-radius: 5px;
  font-weight: 700;
  cursor: pointer;
}
.form-os {
  margin-top:20px
}
.form-os input[type="email"] {
  margin-bottom:20px;
}
.form-os input{
  border-radius: 8px !important;
  padding-left: 20px !important;
  padding-right: 20px !important;
}
.info-prices {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
}
#shopify-section-page-order-summary {
  max-width: 550px;
  margin-left: auto;
  margin-right: auto;
  margin-top: 75px;
  background: linear-gradient(to bottom,  rgba(208,168,92,0.2) 0%,rgba(255,255,255,0.2) 40%,rgba(255,255,255,1) 70%,rgba(208,168,92,0.2) 100%)!important;
  padding: 40px 40px 68px;
}
.wrapper-discount-code {
  display:flex;
}
.list-order li{
  list-style: none;
  display: flex;
  justify-content: space-between;
  gap: 20px;
}
.list-order li:not(:last-child){
  margin-bottom:15px;
}
.list-order .image-product{
  width: 110px;
  height: 110px;
  border-radius: 8px;
  border: 1px rgba(0,0,0,0.1) solid;
  background-color: white;
  position: relative;
}
.list-order .image-product img{
  width: 110px;
  height: 110px;
  padding:10px;
  min-width: 110px;
  max-width: 110px;
  min-height: 110px;
  max-height: 110px;
}
.list-order .box-content{
  flex-grow: 2;
  position:relative;
  align-items: center;
  display: flex;
}
.list-order .variant{
  margin-top: 5px;
  padding-bottom: 20px;
  font-size: 12px;
}
.qlt-count {
  background-color: #6c757d;
  width: 23px;
  height: 23px;
  font-size: 12px;
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  position: absolute;
  top: -6px;
  right: -11px;
  color: white;
}
.list-order .price {
  position: absolute;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
}
.list-order {
  overflow-y: scroll;
  max-height: 518px;
  padding-top: 10px;
  padding-bottom: 30px;
  padding-right: 15px;
}
.title, .price {
  color:#323232;
}
.list-order .heading{
  padding-right: 60px;
}
</style>